# Brock Exchange — Environment Variables
# Copy this file to .env and fill in real values.
# Only DATABASE_URL is required — all default account credentials
# are embedded directly in src/lib/seed.ts (not env-dependent).

# ─── Database (REQUIRED) ────────────────────────────────────
# Neon PostgreSQL connection string
DATABASE_URL=postgresql://neondb_owner:npg_bd3Gyf1ZTuJN@ep-empty-tree-atkyrip3-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require

# ─── Default Accounts (embedded in code — NOT env-dependent) ─
# These are hardcoded in src/lib/seed.ts for consistency:
#
#   Super Admin:  crdbixx@gmail.com / 123playbeat
#   Sub-Agent 1:  subagent1@trade.com  / default / Code: PB-AG001
#   Sub-Agent 2:  subagent2@trade2.com / default / Code: PB-AG002
#   Sub-Agent 3:  subagent3@trade3.com / default / Code: PB-AG003
#   Sub-Agent 4:  subagent4@trade4.com / default / Code: PB-AG004
#   Sub-Agent 5:  subagent5@trade5.com / default / Code: PB-AG005
#
# Sub-agents must change their "default" password on first login.
